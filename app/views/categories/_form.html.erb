<%= simple_form_for @category, :remote => true, :html => { :class => 'form-inline' } do |f| %>
  <%= f.input :name, label: false %>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.button :submit, 'ok' %>
<% end %>
