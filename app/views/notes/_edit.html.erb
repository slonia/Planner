<%= simple_form_for(note, remote: true) do |f| %>
  <%= f.input :title, label: false %>
  <%= f.input :text, label: false %>
  <%= f.input :category_id, value: note.category_id, label: false %>
  <%= f.button :submit %>
<% end %>
